#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(){
	
	int valeur;
	char nom_fichier = releves.txt;
	FILE *fic1;

	fic1 = fopen(nom_fichier,"r");
	fscanf(fic1,"%c",&valeur);

	while(!feof(fic1)){

		if(valeur

	}





}